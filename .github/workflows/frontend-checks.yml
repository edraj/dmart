name: CXB checks

on:
  push:
    branches: [ "master" ]
    paths: ["cxb/**"]
  pull_request:
    branches: [ "master" ]
    paths: ["cxb/**"]

permissions:
  contents: read

jobs:
  build:

    runs-on: self-hosted

    steps:
    - name: Install cxb js depdenencies
      run: |
        cd cxb
        yarn install
    - name: Build CXB 
      run: |
        cd cxb
        cp public/config.sample.json public/config.json
        yarn build
    - name: Check svelte
      run: |
        cd cxb
        yarn svelte-check
